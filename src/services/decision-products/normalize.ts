// src/services/decision-products/normalize.ts

import type { DecisionProduct } from "./search";

export function normalizeProducts(products: DecisionProduct[]): DecisionProduct[] {
  // Sprint 2: normalizar campos, limpiar strings, deduplicar.
  return products;
}
