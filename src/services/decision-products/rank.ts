// src/services/decision-products/rank.ts

import type { DecisionProduct } from "./search";

export function rankProducts(products: DecisionProduct[]): DecisionProduct[] {
  // Sprint 2: ranking (por relevancia, reseñas si existen, etc.)
  return products;
}
